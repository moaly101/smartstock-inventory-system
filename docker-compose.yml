version: '3.8'
services:
  db:
    image: postgres:15
    container_name: smartstock-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: smartstock
    ports:
      - "5432:5432"